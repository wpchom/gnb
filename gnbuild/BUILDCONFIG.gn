declare_args() {
  gnb_repo_dir = getenv("GNB_REPO_DIR")
}

declare_args() {
  gnb_pkgs_dir = rebase_path("${gnb_repo_dir}/packages")
}

# package
import("${gnb_repo_dir}/gnbuild/package.gni")

# toolchain
import("${gnb_repo_dir}/gnbuild/toolchain.gni")

# default
target_os = host_os
target_cpu = host_cpu
